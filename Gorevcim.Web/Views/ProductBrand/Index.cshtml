
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-12">
    <div class="card">
        <div class="card-body">

            <div class="table-responsive">
                <table class="table table-editable table-nowrap align-middle table-edits">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>BrandName</th>
                            <th>LogoBase64Content</th>
                            <th>LogoFileName</th>
                            <th>LogoFilePath</th>
                            <th>ProductFeaturesId</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Id</td>
                                <td>@item.BrandsName</td>
                                <td>@item.LogoBase64Content</td>
                                <td>@item.LogoFileName</td>
                                <td>@item.LogoFilePath</td>
                                <td>@item.ProductFeaturesId</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

        </div>
    </div>
</div>

